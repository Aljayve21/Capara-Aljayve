import { NextDateDirective } from './nex-date.directive';

describe('NextDateDirective', () => {
  it('should create an instance', () => {
    const directive = new NextDateDirective();
    expect(directive).toBeTruthy();
  });
});
